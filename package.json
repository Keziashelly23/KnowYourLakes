{
  "scripts": {
    "start:backend": "node --use-system-ca backend/node.js",
    "start:frontend": "live-server frontend --host=localhost --port=3000"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^4.18.2",
    "google-auth-library": "^10.6.2"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
